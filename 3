#include <iostream>

using namespace std;

int n,v[41],i,nr,ok=0;

int main()
{
    cin>>nr;
    v[1]=1;
    v[2]=1;
    for(i=3;v[i]<=nr;i++)
    {
        v[i]=v[i-1]+v[i-2];
        if(v[i]==nr)
        {
            ok=1;
            break;
        }
    }
    if(ok==1)
    {
        cout<<"DA";
    }
    else
    {
        cout<<"NU";
    }
    // DA sau NU numar 

    
    return 0;
}
